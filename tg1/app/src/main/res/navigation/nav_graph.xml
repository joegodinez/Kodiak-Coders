<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.tg1.MainActivity"
        android:label="activity_2"
        tools:layout="@layout/home" />
    <activity
        android:id="@+id/login"
        android:name="com.example.tg1.login"
        android:label="activity_login"
        tools:layout="@layout/activity_login" />
    <activity
        android:id="@+id/create_account"
        android:name="com.example.tg1.create_account"
        android:label="activity_create_account"
        tools:layout="@layout/activity_create_account" />
</navigation>